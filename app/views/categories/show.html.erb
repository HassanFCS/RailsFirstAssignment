<h1 class="text-center mt-4"><%=@category.name%></h1>



<div class="text-center mt-4">
  <%= link_to "Edit Category Name", edit_category_path(@category), class: "btn btn-outline-info" if logged_in? && current_user.admin?%>
</div>

<h3 class="text-center mt-4">Articles</h3>

<div class="flickr_pagination mt">
    <%= will_paginate @articles, :container => false %>
</div>

<%=render 'articles/article' %>

<div class="flickr_pagination mt-3">
    <%= will_paginate @articles, :container => false %>
</div>